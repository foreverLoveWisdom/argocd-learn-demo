# argocd-learn-demo

Throwaway repo for learning GitOps with ArgoCD on a local k3s cluster.

Contains one `deployment.yaml` (nginx) used as the "desired state" ArgoCD watches
and syncs to the cluster. No real workload — practice repo only.
